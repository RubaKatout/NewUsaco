#include<bits/stdc++.h>
/*
Created By Tornado2004💚💚
*/
using namespace std;

#define debug(x) cout << '[' << #x << " is: " << x << "] " << endl;
#define imod(a , n) (a % n + n ) % n

#define fastio ios_base::sync_with_stdio(false);cin.tie(0);
#define inF freopen("teleport.in","r",stdin );
#define outF freopen("teleport.out" , "w" , stdout ) ;
#define sor(v) sort(v.begin() , v.end());
#define print(v) for(auto f : v ) cout << f << " " ;
#define rsor(v) sort(v.rbegin() , v.rend());
#define rev(v) reverse(v.begin() , v.end());

#define ll long long

#define int ll
#define pii pair<int , int >
const int N = 1e5 + 5  , K = 11 ;
const ll MOD = 1e9 + 7 , oo = 1e9 , OO = 1e18 ;
const double pi = acos(-1) , eps = 1e-6 ;

//37


int di[] = {1 , -1 , 0 , 0};
int dj[] = {0 , 0 , 1 , -1};

string arr[33][33];

int32_t main()
{   //inF;
    //inF;outF;
    fastio;
    // Dont Forget memset


    int a , b , c , d;
    cin >> a >> b >> c >> d ;

    cout << min({abs(a - b) , abs(a - d) + abs(b - c) , abs(a - c) + abs(b - d) } );

}
